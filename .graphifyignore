# Что НЕ является архитектурой UnboxCRM.
# Синтаксис как у .gitignore. graphify читает этот файл автоматически.
#
# node_modules/, dist/, .venv/, .git/, graphify-out/ и lock-файлы graphify
# пропускает сам (встроенный _SKIP_DIRS) — здесь их дублировать не нужно.

# --- Инструментарий агента, а не код продукта ---------------------------
# Давал 2258 из 5431 узлов (42% графа) и делал live-browser.js самым
# связанным узлом всей системы. Это скрипты скиллов, а не Unbox.
.claude/

# --- Минифицированные бандлы --------------------------------------------
# После минификации функции зовутся v(), q(), P(). Экстрактор склеивал их
# с реальными компонентами и рождал ложные INFERRED-рёбра
# (ChessboardScroller -> el(), LinkBookingModal -> v()).
*.min.js
*.umd.js

# --- Конфиг-манифесты ----------------------------------------------------
# Давали 147 узлов из ключей конфигов (name, version, dev, build) и имён
# npm-пакетов. ~17 «сообществ» графа были просто названиями библиотек.
# Список зависимостей и так виден в самих этих файлах.
/package.json
/tsconfig.json
/tsconfig.app.json
/tsconfig.node.json
/eslint.config.js
/postcss.config.js
/vite.config.ts

# --- Изображения ---------------------------------------------------------
# Фото кабинетов, иконки, favicon. В графе им место только если бы мы строили
# граф контента, а не кода: в прошлом графе они дали ровно 0 узлов, но detect
# их подхватывает и это 128 vision-вызовов впустую.
*.png
*.jpg
*.jpeg
*.webp
*.svg
*.gif
*.ico

# --- Дубликаты и тестовые артефакты --------------------------------------
# ADMIN_GUIDE.html — сгенерированная копия ADMIN_GUIDE.md: тот же текст
# попал бы в граф дважды, двумя наборами узлов.
/docs/ADMIN_GUIDE.html
/telegram_test.html
/public/robots.txt
# Снапшот уже применённой миграции, лежит как .txt
*.applied_*.py.txt

# --- Артефакты -----------------------------------------------------------
/database.db
raw/
