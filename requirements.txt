# Единственный источник правды — backend/requirements.txt.
#
# Раньше здесь лежала копия того списка, и она разъехалась: не хватало slowapi,
# который main.py импортирует на уровне модуля. Пересоздание дроплета по этому
# файлу дало бы ModuleNotFoundError при старте бэкенда — ровно в тот момент,
# когда времени разбираться меньше всего.
-r backend/requirements.txt
